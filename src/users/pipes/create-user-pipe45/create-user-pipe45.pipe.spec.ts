import { CreateUserPipe45Pipe } from './create-user-pipe45.pipe';

describe('CreateUserPipe45Pipe', () => {
  it('should be defined', () => {
    expect(new CreateUserPipe45Pipe()).toBeDefined();
  });
});
